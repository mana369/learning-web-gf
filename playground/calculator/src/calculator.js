

class Operator {
    addValues;
    substractValues;
    multiplicateValues;
    divideValues;
    deleteValues;
    equal;
    
    constructor(addValues, substractValues, multiplicateValues, divideValues, deleteValues, equal) {
        this.addValues = addValues;
        this.substractValues = substractValues;
        this.multiplicateValues = multiplicateValues;
        this.divideValues = divideValues;
        this.deleteValues = deleteValues;
        this.equal = equal;
    }

    getaddValues() {
        if (button.addEventListener(click) && operationBtn.addEventListener('click', )) {
            Number(number + number);
        }; 
    };

    
}

class Calculator {
    inputValue;
    storedValue;
    operator;

    constructor(inputValue, storedValue, operator) {
        this.inputValue = inputValue;
        this.storedValue = storedValue;
        this.operator = Operator;
    }
}